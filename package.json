{
  "name": "svg-api-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "echo 'API-only project: no static build needed'",
    "dev:api": "vercel dev --listen 3000"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3",
    "@vercel/node": "^3.0.20"
  }
}
